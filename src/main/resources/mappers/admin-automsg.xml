<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sdtalk.admin.automsg">
	
	<!-- 자동메세지 목록 조회 -->
	<select id="selectAll" parameterType="map" resultType="map">
		select * 
		from TemplateAuto 
		where cid = #{cid}
		and type = #{type}
	</select>
	
	<!-- 자동메세지 목록 조회 -->
	<select id="selectOne" parameterType="map" resultType="map">
		select *
		from TemplateAuto 
		where cid = #{cid}
		and id = #{id}
	</select>
	
	<!-- 자동메세지 정보 등록 -->
	<insert id="insert" parameterType="map" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		insert into TemplateAuto (cid, type, msg, emp)
		values (#{cid}, #{type}, #{msg}, #{emp})
	</insert>
	
	<!-- 자동메세지 정보 수정 -->
	<update id="update" parameterType="map">
		update TemplateAuto 
		set msg = #{msg}
		where id = #{id}
	</update>
	
	<!-- 자동메세지 정보 삭제 -->
	<delete id="delete">
		delete 
		from TemplateAuto 
		where id = #{id}
	</delete>
</mapper>