<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sdtalk.admin.emp">
	<!-- 계정 카운트 조회 -->
	<select id="selectCount" parameterType="map" resultType="int">
		<![CDATA[
			select count(*) as cnt 
			from Emp e 
				inner join Speaker s on e.speaker = s.id 
			where e.cid = #{cid} 
			and e.auth > 1
		]]>
	</select>
	
	<!-- 계정목록 조회 -->
	<select id="selectAll" parameterType="map" resultType="map">
		<![CDATA[
			select s.name, e.id, e.cid, e.speaker, e.auth, e.empno as empno, e.createdate, e.workdate, e.profileimg, e.state
			from Emp e 
				inner join Speaker s on e.speaker = s.id
			where e.cid = #{cid} 
			and e.auth > 1
			order by e.id desc
		]]>
	</select>
	
	<!-- 계정정보 등록 -->
	<insert id="insert" parameterType="com.scglab.connect.services.admin.emp.Emp">
		INSERT INTO Emp(createdate, workdate, cid, speaker, auth, profileimg, empno, state)
		VALUES(CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, #{cid}, #{speaker}, #{auth}, #{profileimg}, #{empno}, #{state});
	</insert>
	
	<!-- 계정정보 수정 -->
	<update id="update" parameterType="map">
		<![CDATA[
			update Emp 
			set cid = #{cid}, 
				empno = #{empno},
				auth = #{auth},
                state = #{state},
                profileimg = #{profileimg}
  			where id = #{id}
		]]>
	</update>
	
	<!-- 계정정보 삭제 -->
	<delete id="delete">
		<![CDATA[
			delete from Emp
			where id = #{id}
		]]>
	</delete>
</mapper>