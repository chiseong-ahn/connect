# 서버 설정.
server:
  port: 8080
  servlet:
    context-path: '/'
    session:
      timeout: 600
      cookie:
        domain: localhost
        path: '/'
  error:
    include-exception: true
    include-stacktrace: never
    path: /error/error
    whitelabel:
      enabled: false

# 스프링부트 기본 설정.
spring:
  application:
    name: 상담톡 v2.0 
  banner:
    charset: UTF-8
    location: 'classpath:config/banner.txt'
  profiles:
    active: local
    web-application-type: none
  jackson:
    serialization:
      INDENT_OUTPUT: true
  messages:
    basename: messages/message
    encoding: UTF-8
  freemarker:
    cache: false
  datasource:
    hikari:
      driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
      #jdbc-url: 'jdbc:log4jdbc:mysql://dev.gasapp.co.kr:3306/cstalk?characterEncoding=UTF-8&serverTimezone=UTC'
      jdbc-url: 'jdbc:log4jdbc:mysql://127.0.0.1:3306/cstalk?autoReconnect=true&characterEncoding=UTF-8&serverTimezone=UTC'
      username: cstalk
      password: cstalk12#$
      idle-timeout: 10000
      connection-timeout: 300000
      maximumPoolSize: 10
      minimum-idle: 5
      max-lifetime: 60000
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

     

# Prometheus 설정.
management:
  endpoints:
    web:
      base-path: /application
      exposure:
        #include: "prometheus"
        include: "*"
  metrics:
    tags:
      application: ${spring.application.name}  


# Swagger 설정.
springdoc:
  cache:
    disabled: true
  api-docs:
    groups:
      enabled: true
  swagger-ui:
    path: /api/document
    displayRequestDuration: true
    groups-order: ASC

relay:
  use-example: false


---
# 로컬환경 
spring:
  profiles: local
  redis:
    host: localhost
    port: 6379
  freemarker:
    template-loader-path: 'classpath:/templates'
    suffix: .ftl
    cache: false
  main:
    web-application-type: servlet
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
      
domain:
  sdtalk: cstalk-local.gasapp.co.kr
  relay-scg: relay-scg-dev.gasapp.co.kr
  relay-inc: relay-inc-dev.gasapp.co.kr
  
path:
  upload: /Volumes/Data/_upload

# Logging 설정.
logging:
  config: 'classpath:config/logback-local.xml'

relay:
  use-example: true
  
---
# 서버1 
server:
  port: 8081
  
spring:
  profiles: server1
  redis:
    host: localhost
    port: 6379
  freemarker:
    template-loader-path: 'classpath:/templates'
    suffix: .ftl
  main:
    web-application-type: servlet

domain:
  sdtalk: localhost
  relay-scg: relay-scg-dev.gasapp.co.kr
  relay-inc: relay-inc-dev.gasapp.co.kr
  
path:
  upload: /Volumes/Data/_upload

# Logging 설정.
logging:
  config: 'classpath:config/logback-server1.xml'

relay:
  use-example: true
  
---
# 서버2
server:
  port: 8082
  
spring:
  profiles: server2
  redis:
    host: localhost
    port: 6379
  freemarker:
    template-loader-path: 'classpath:/templates'
    suffix: .ftl
  main:
    web-application-type: servlet

domain:
  sdtalk: localhost
  relay-scg: relay-scg-dev.gasapp.co.kr
  relay-inc: relay-inc-dev.gasapp.co.kr
  
path:
  upload: /Volumes/Data/_upload

# Logging 설정.
logging:
  config: 'classpath:config/logback-server2.xml'

relay:
  use-example: true

---
# 개발환경 
server:
  port: 8115
  
spring:
  profiles: dev
  redis:
    host: 127.0.0.1
    port: 6379
  freemarker:
    template-loader-path: 'classpath:/templates'
    suffix: .ftl
  main:
    web-application-type: servlet

domain:
  sdtalk: cstalk-dev.gasapp.co.kr
  relay-scg: relay-scg-dev.gasapp.co.kr
  relay-inc: relay-inc-dev.gasapp.co.kr
  
path:
  upload: /data/project/gasapp-cstalk/apache/data/attach

logging:
  config: 'classpath:config/logback-dev.xml'

relay:
  use-example: true
---
# 운영환경 
server:
  port: 8080

spring:
  profiles: live
  redis:
    host: 127.0.0.1
    port: 6379
  freemarker:
    template-loader-path: 'classpath:/templates'
    suffix: .ftl
  main:
    web-application-type: servlet

domain:
  sdtalk: cstalk.gasapp.co.kr
  relay-scg: relay-scg.gasapp.co.kr
  relay-inc: relay-inc.gasapp.co.kr
  
path:
  upload: /data/project/cstalk/apache/data/attach

# Logging 설정.
logging:
  config: 'classpath:config/logback-live.xml'
  
relay:
  use-example: false